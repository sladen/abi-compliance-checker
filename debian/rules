#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	mkdir -p debian/$(shell dh_listpackages)/usr/bin/
	cp abi-compliance-checker.pl debian/$(shell dh_listpackages)/usr/bin/abi-compliance-checker
